# Parameter Table

## 1. 通用参数

| 参数 | 当前值 | 说明 |
|---|---:|---|
| 串口监视器波特率 | 115200 | PlatformIO monitor |
| ESP-NOW 信道 | 1 | 两端固定 |
| ESP-NOW 加密 | false | 当前未加密 |

## 2. 发射端参数

| 参数 | 当前值 | 说明 |
|---|---:|---|
| 控制发送频率 | 100Hz | 控制包发送 |
| 状态接收超时 | 1000ms | 判定接收端断线；接收端 failsafe 仍为 2000ms |
| 摇杆 ADC 分辨率 | 12bit | `0 ~ 4095` |
| 摇杆中心值 | 2048 | 当前固定值，后续改开机校准 |
| 摇杆死区 | 50 | ADC count |
| 摇杆输出范围 | `-1000 ~ 1000` | 控制包 throttle |
| OLED I2C 地址 | `0x3C` | SSD1306 |
| OLED 刷新周期 | 100ms | 10Hz |
| CW2015 I2C 地址 | `0x62` | 本地电量计 |
| CW2015 读取频率 | 2Hz | 本地电压和 SOC |
| 低电量阈值 | 20% | 首次报警 |
| 严重低电量阈值 | 10% | 周期报警 |
| 按键提示频率 | 1800Hz | 短按确认音 |
| 开机提示频率 | 2200Hz | 上电提示音 |
| 断线报警频率 | 1200Hz | 接收端断线时周期报警 |
| 低电量报警频率 | 700Hz | CW2015 本地电量低时报警 |

## 3. S3 正式发射端参数

| 参数 | 当前值 | 说明 |
|---|---:|---|
| 已连接控制发送频率 | 100Hz | 收到接收端状态包后使用，保持控制响应 |
| 未连接搜索发送频率 | 20Hz | 接收端未开机或断联时使用，降低 ESP-NOW 空发功耗 |
| 状态接收超时 | 500ms | 判定接收端断线 |
| LVGL 显示刷新周期 | 16ms | `LV_DISP_DEF_REFR_PERIOD`，约 60Hz，改善触摸点跟手 |
| LVGL 触摸读取周期 | 5ms | `LV_INDEV_DEF_READ_PERIOD`，提高触摸采样响应 |
| LVGL 页面数据更新周期 | 200ms | 更新标签和油门条，避免业务文本频繁刷新 |
| LVGL handler 调度周期 | 2ms | 一级低延时配置，减少触摸事件等待时间 |
| LVGL 显示 DMA | 开启 | `pushImageDMA()`，刷新完成前等待 DMA，避免 LVGL 缓冲区被提前复用 |
| LVGL FPS 采样周期 | 1000ms | 页面底部显示 `FPS n`，按 flush 完成帧数折算 |
| LVGL RGB565 字节交换 | 开启 | `LV_COLOR_16_SWAP=1`，适配 8-bit 并口屏颜色传输顺序 |
| 主循环空闲延时 | 2ms | 一级低延时配置，兼顾触摸响应和空转占用 |
| CPU 频率 | 160MHz | S3 正式端启动后设置 |
| 屏幕背光 | 140/255 | 降低发热和功耗，后续可按目视效果调整 |
| WiFi 发射功率 | 19.5dBm | 提高 S3 ESP-NOW 链路余量；若发热明显再评估分档调节 |
| S3 AUX I2C | GPIO18/GPIO19 | SDA/SCL，挂载 CW2015、BMP280、QMC5883L，LSM6DSLTR 待确认 |

## 4. 接收端参数

| 参数 | 当前值 | 说明 |
|---|---:|---|
| 遥测回传频率 | 20Hz | 状态包发送 |
| 失控保护超时 | 2000ms | 未收到控制包 |
| PWM 频率 | 50Hz | VESC Servo/PWM |
| PWM 分辨率 | 10bit | LEDC |
| PWM 最小值 | 51 | 约 1.0ms |
| PWM 最大值 | 102 | 约 2.0ms |
| 1档限幅 | 500 | throttle 绝对值上限 |
| 2档限幅 | 750 | throttle 绝对值上限 |
| 3档限幅 | 1000 | throttle 绝对值上限 |
| VESC UART 波特率 | 115200 | `Serial1` |
| VESC 读取周期 | 1000ms | 电压、RPM |
| 速度系数 | 0.00207 | 已实测，`speed = rpm * 0.00207` |
| 开机提示频率 | 2200Hz | 上电提示音 |
| 连接成功提示频率 | 2400Hz | 从未连接/失控恢复到已连接 |
| 链路异常提示频率 | 1200Hz | 遥控器未打开、断电或失效 |
| 失控保护提示频率 | 900Hz | 进入 failsafe |
| 远程蜂鸣频率 | 2600Hz | 按钮 2 远程触发 |
| 远程蜂鸣最长持续时间 | 3000ms | 超时后自动停止 |

## 5. 参数变更规则

1. 任何参数变更必须记录原因。
2. 影响通信协议的参数变更必须同步更新 `protocol.md`。
3. 影响硬件行为的参数变更必须同步更新 `test-plan.md`。
4. 轮速系数已实测，除非重新测量，不应随意修改。
