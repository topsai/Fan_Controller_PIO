# User Guide

本文件面向实际使用和调试，说明这套发射端/接收端系统怎么开机、怎么操作、有哪些提示音和保护行为。

## 1. 系统组成

| 角色 | PlatformIO 环境 | 端口约定 | 作用 |
|---|---|---|---|
| 发射端/遥控器 | `transmitter` | COM3 | 读取摇杆、三档开关、按钮、电池信息，通过 ESP-NOW 发送控制数据 |
| 接收端/接收机 | `receiver` | COM10 | 接收控制数据，输出 PWM/Servo 给 VESC，读取 VESC 电压和转速并回传 |

## 2. 正常使用步骤

1. 确认接收端和 VESC 共地，VESC UART、PWM/Servo 信号线连接正常。
2. 打开发射端电源，保持摇杆自然回中。
3. 发射端上电时会自动采样摇杆中位，不要在刚开机时推动摇杆。
4. 打开接收端电源。
5. 发射端 OLED 显示 `[OK]` 后表示已经收到接收端回传。
6. 推动摇杆控制油门/刹车。
7. 使用三档开关选择速度限幅。
8. 使用按钮 2 可以让接收端蜂鸣器响起；单次最长 3 秒，松开后可重新触发。

## 3. 发射端 OLED 显示

| 显示内容 | 含义 |
|---|---|
| `[OK]` | 已收到接收端状态包，链路正常 |
| `[LOST]` | 超过 500ms 未收到接收端状态包 |
| `BAT:x.xxV` 第一行 | 接收端/VESC 回传电压 |
| `SPD:n` | 当前速度档位 |
| `THR:xxxx` | 正向油门值 |
| `BRK:xxxx` | 反向/刹车值 |
| 大号 `xx KM` | 接收端回传速度 |
| `SOC:xxx% BAT:x.xxV` | 发射端本地 CW2015 电量和电压 |

## 4. 发射端操作

### 4.1 摇杆

| 操作 | 行为 |
|---|---|
| 自然回中 | 输出 throttle 约为 0 |
| 正向推动 | 输出 `0 ~ 1000` |
| 反向推动 | 输出 `0 ~ -1000` |

发射端每次上电会采样 64 次摇杆 ADC 作为中位。开机时不要触碰摇杆，否则中位会被校准偏。

### 4.2 三档开关

| 档位 | 接收端 throttle 限幅 |
|---|---:|
| 1 档 | `-500 ~ 500` |
| 2 档 | `-750 ~ 750` |
| 3 档 | `-1000 ~ 1000` |

如果多个档位输入同时有效，优先级为 `3 > 2 > 1`。如果没有档位输入有效，默认 1 档。

### 4.3 按钮

| 按钮 | 当前功能 |
|---|---|
| 按钮 1 | 协议保留，当前没有业务功能 |
| 按钮 2 | 按下后接收端蜂鸣器响起；单次最长 3 秒，松开后可重新触发 |

两个按钮按下时，发射端本地会短鸣作为按键提示。

## 5. 接收端行为

### 5.1 PWM/Servo 输出

接收端 GPIO4 输出 50Hz PWM/Servo 信号给 VESC。

| throttle | duty | 估算脉宽 |
|---:|---:|---:|
| `-1000` | 51 | 约 1.0ms |
| `0` | 76 | 约 1.5ms |
| `1000` | 102 | 约 2.0ms |

这符合常见 VESC PPM/Servo 输入范围。最终是否匹配实际 VESC 设置，以 VESC Tool 的 PPM 校准页面为准。

### 5.2 VESC 遥测

| 数据 | 来源 | 用途 |
|---|---|---|
| 输入电压 | VESC UART | 回传发射端 OLED 显示 |
| RPM | VESC UART | 换算速度 |
| 速度 | `rpm * 0.00207` | 已实测系数 |

接收端不再使用 ADC 读取电池电压。

## 6. 提示音和报警

### 6.1 发射端提示

| 场景 | 行为 |
|---|---|
| 开机 | 两次短鸣 |
| 按钮按下 | 短鸣 |
| 接收端断线 | 周期报警 |
| 发射端电量低于 20% | 首次三短一长提示 |
| 发射端电量低于 10% | 每 1 秒蜂鸣约 500ms |

### 6.2 接收端提示

| 场景 | 行为 |
|---|---|
| 开机 | 两次短鸣 |
| 按钮 2 按下 | 远程蜂鸣，单次最长 3 秒 |
| 按钮 2 松开 | 停止蜂鸣，并复位远程蜂鸣超时保护 |
| 遥控器未打开/断电/失效 | 每 2 秒短促蜂鸣一次 |
| 进入失控保护 | throttle 清零，档位回 1，按钮清零，并蜂鸣提示 |

已实测：手动关闭发射器后，接收端会发出短促间隔的滴滴声。

注意：按钮 2 的远程蜂鸣不是无限时长。如果接收端连续识别到按钮 2 按下超过 3 秒，会自动停止本次蜂鸣，并在串口输出 `远程蜂鸣超时保护，已停止`。需要再次蜂鸣时，先松开按钮 2 再重新按下。

## 7. 失控保护

接收端超过 2000ms 未收到合法控制包后进入失控保护。

进入失控保护时，接收端状态会统一设置为：

| 字段 | 安全值 |
|---|---|
| `throttle` | `0` |
| `speedLevel` | `1` |
| `buttons` | `0` |
| `connected` | `false` |
| `failsafeActive` | `true` |

此时 PWM 输出回到中位附近，约 1.5ms。

## 8. LED 状态

| 状态 | LED 行为 |
|---|---|
| 收到合法控制包 | 每次收包翻转 |
| 未连接 | 约 500ms 慢闪 |
| 失控保护 | 约 100ms 快闪 |

## 9. 常用 PlatformIO 命令

### 9.1 自动测试

```text
pio test -e native
```

### 9.2 编译

```text
pio run -e transmitter
pio run -e receiver
```

### 9.3 上传固件

```text
pio run -e transmitter -t upload --upload-port COM3
pio run -e receiver -t upload --upload-port COM10
```

### 9.4 串口查看

```text
pio device monitor -p COM3 -b 115200
pio device monitor -p COM10 -b 115200
```

## 10. 每次改代码后的固定流程

1. 运行 `pio test -e native`。
2. 运行 `pio run -e transmitter`。
3. 运行 `pio run -e receiver`。
4. 上传发射端到 COM3。
5. 上传接收端到 COM10。
6. 做相关硬件复测。
7. 更新 `docs/progress.md`、`docs/bugs.md` 和 `docs/test-runs/`。
8. 提交并推送到 GitHub。

## 11. 常见问题

| 问题 | 检查方向 |
|---|---|
| OLED 显示 `[LOST]` | 检查接收端是否上电、MAC 是否匹配、两端是否同信道 |
| 接收端一直滴滴响 | 发射端可能未开机、断电、距离过远或 ESP-NOW 未连上 |
| 接收端出现长鸣 | 检查按钮 2 是否被按住或卡住；当前固件会在连续 3 秒后自动停止远程蜂鸣 |
| 摇杆回中不为 0 | 重新上电并确保开机时摇杆保持自然回中 |
| VESC 没响应 | 检查 GPIO4 PWM、VESC PPM/Servo 配置、共地和输入校准 |
| 没有 VESC 电压/速度 | 检查 UART 线序、波特率 115200、VESC 是否供电 |
| 串口没有应用日志 | 确认 `platformio.ini` 中启用了 `ARDUINO_USB_MODE=1` 和 `ARDUINO_USB_CDC_ON_BOOT=1` |
